# goal-loop v2.0 重写实施计划

## 设计（已批准）

身份：**/goal 的驱动 + /team 的 crew + 一道验收门**。评估器（haiku 读 transcript）
替换为 crew 执行 + 门控测树；状态从 transcript 换成磁盘账本。

四步循环：契约（AC+命名检查，用户批准）→ crew 派工（≤3 并发 subagent，作用域
不相交，接口契约进简报）→ join+gate（合并审阅→门控重跑确定性检查）→ 分岔
（GO 交付 / NO-GO 从账本取下一批继续——不停，四类例外：不可逆/安全/共享副作用/契约需修）。

砍：modes.md（预算模式矩阵）、逐轮 judged 席位（改 claim 前终验一次）、
INTEGRATION.md（并入 SKILL/README）、fail-cap-BLOCKED（改四类停车+熔断）。
留：契约 DSL、门控重跑、账本格式（v1.x 兼容）、熔断、冷席终验、R1-R8 精简版。

文件结构（7+tests）：
SKILL.md（身份+循环+纪律）· references/{gate,crew,patterns}.md ·
scripts/{goal_gate.sh,goal_ctl.sh}（逻辑不动）· assets/goal.contract.md · tests/

## 任务

- T1 SKILL.md v2.0 重写（身份/循环/契约/crew/驱动纪律/终验/熔断/账本）
- T2 references/gate.md（exit-gate.md 精简迁移）+ crew.md（新：crew 协议+终验+简报模板+比较式判定）
  + patterns.md（domain-patterns.md 原样迁移）
- T3 删除 modes.md / checker-panel.md / exit-gate.md / INTEGRATION.md
- T4 docs_consistency.sh 适配（C2 去掉 INTEGRATION、C4 随 modes.md 删除）
- T5 run_tests.sh 适配（若有 grep INTEGRATION/modes 的断言）；全套跑绿
- T6 VERSION→2.0.0；仓库 README 更新新身份；同步提交推送
- T7 验收实验（下一会话）：同 b747 提示词 v2.0 vs /goal 对照

## 验收

- 全套测试绿 + docs consistency CONSISTENT
- SKILL.md 自检：身份一节存在、四步循环完整、被砍机制无残留引用
- 推送成功
